application:
    name: ${APP_NAME}
    project: ${APP_NAME}
    publicly_accessible: true
  routers:
    - name: main
      routes:
        - application_name: ${APP_NAME}
          paths:
            - /
